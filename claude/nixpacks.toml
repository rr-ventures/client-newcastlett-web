# Railway/Nixpacks configuration for static site
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["echo 'Static site - no build needed'"]

[start]
cmd = "npx http-server . -p ${PORT:-3000}"
